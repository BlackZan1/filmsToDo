import styled from "styled-components";

export const ContainerLayout = styled.div`
    width: 50%;
    min-height: 100vh;
    padding-top: 60px;
    display: flex;
    flex-direction: column;
    overflow-x: hidden;
    position: relative;
`